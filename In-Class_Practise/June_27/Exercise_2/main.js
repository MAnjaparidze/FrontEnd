arr = [-2, -3, 4, -1, -2, 1, 5, -3];

function getMaxSubSu(arr) {
    var arraySum = 0;
    for (let i = 0; i < arr.length; i++) {
        arraySum = arraySum + arr[i];     
    }
    for (let y = 0; i < arr.length; y++) {
        // if (){
            
        // }
    }
}
getMaxSubSu(arr);